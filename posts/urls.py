from django.urls import path
# from .views import ImageDetailView, ImageListCreateView, NoteListCreateView, NoteDetailView

urlpatterns = [
    # path('new/', NoteListCreateView.as_view(), name="create_note"),
    # path('note/<int:pk>/', NoteDetailView.as_view(), name="note_detail"),
    # path('note/images/<int:note_id>/',
    #      ImageListCreateView.as_view(), name="image_list_create"),
    # path('note/image-detail/<int:pk>/',
    #      ImageDetailView.as_view(), name="image_detail"),
]
